# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: Tracking ID
        searchable: true

    subject:
        label: Subject
        searchable: true

    status:
        label: Status
        searchable: true

    updated_at:
      label: Last Updated On
      type: datetime
      format: D, d M Y H:i:s
      searchable: true
